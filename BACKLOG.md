# Backlog — Audiodocs

> **Para agentes:** Este archivo es la fuente de verdad del backlog. Léelo al inicio de cada sesión. Al completar un ítem, muévelo a `## ✅ Resueltos` e incluye la actualización en el mismo commit del fix. Al descubrir un bug o mejora durante el trabajo, agrégalo aquí con prioridad y esfuerzo estimado.

---

## 🔴 Alta prioridad

_(vacío)_

---

## 🟡 Media prioridad

| # | Título | Descripción | Esfuerzo |
|---|--------|-------------|----------|
| S8 | Rate limiting: store en memoria por instancia serverless | Mitad pendiente de S5 (ver Resueltos): el `Map` de `rate-limit.ts` vive en memoria de cada instancia serverless — el límite real es N × lambdas activas y se resetea en cada cold start. Migrar a un store compartido (Upstash Redis o Vercel KV) requiere que el usuario provisione el servicio (cuenta, plan, credenciales vía env vars) antes de poder implementarlo — no es una decisión técnica que se pueda resolver sin esa info | M |
| S9 | CSP con nonce en vez de `'unsafe-inline'` para los scripts del theme/splash | Mitad no resuelta de S6 (ver Resueltos): la CSP actual usa `'unsafe-inline'` en `script-src`/`style-src` para acomodar los 2 `&lt;script&gt;` inline de `layout.tsx` (anti-flash de tema + skip de splash). Un CSP con nonce de verdad requeriría `proxy.ts` en la raíz (el nuevo nombre de `middleware.ts` en esta versión de Next) generando un nonce por request — pero Next exige que **todas** las páginas sean dinámicas para poder inyectarlo, lo que sacaría a `/`, `/about`, `/registro` y `/app/archive` de prerender estático (hoy son `○ Static` en el build). Es un trade-off de rendimiento/costo real, no una decisión técnica automática — requiere que el usuario decida si vale la pena antes de implementarlo | M |
| P4b | `AudioPlayerContext.tsx` value sin memoizar (parte pendiente de P4) | Intentado en sesión 2026-07-14 y revertido: `playArticle`/`speakParagraph`/`playEdgeParagraph`/`handleStop`/etc. son mutuamente recursivas (`playArticle` llama a `playEdgeParagraph`/`speakParagraph`, que llaman de vuelta a `playArticle`/`handleStop`), así que un `useCallback` directo por función choca con referencias circulares de inicialización (TDZ) entre sí. Se probó el patrón de indirección por `ref` (mismo truco que ya usaba el código para el efecto de Media Session, generalizado a las 13 funciones expuestas por el contexto) para lograr identidad estable sin ese problema — pero `eslint-plugin-react-hooks` (reglas de diagnóstico del React Compiler, activas en este proyecto vía `eslint-config-next/core-web-vitals` **aunque `reactCompiler` no está habilitado en `next.config.ts`**) lo rechaza con error real (`react-hooks/preserve-manual-memoization`: "Could not preserve existing memoization" — el compiler no puede verificar que llamar a `ref.current.algo()` sea seguro de memoizar). La versión original del archivo (sin ningún intento de arreglo) pasa este mismo lint sin errores; el intento de fix no. Conclusión: resolver esto bien requiere o (a) evaluar habilitar `reactCompiler: true` de verdad (instalar `babel-plugin-react-compiler`) — cambio de alcance de proyecto entero, no de este ítem — o (b) un refactor mayor que rompa la recursión mutua entre esas ~13 funciones de forma compatible con el compiler. Ninguna de las dos es "S". La mitad de P4 que sí se resolvió (memoizar las listas derivadas de `AppClient`) quedó en Resueltos como P4 | M |
| P7 | Caché de TTS/scrape con store compartido entre instancias serverless | Mitad no resuelta de P5 (ver Resueltos): el `MemoryCache` nuevo vive en memoria de cada instancia serverless — no persiste entre lambdas distintas ni sobrevive un cold start. Se investigó el mecanismo nativo de esta versión de Next (`"use cache"`, reemplazo de `unstable_cache` en Next 16) como alternativa sin infra propia, pero su propia documentación confirma que en serverless "cache entries typically don't persist across requests" — mismo problema, no resuelve nada real. Un store de verdad requiere provisionar Vercel KV/Blob o Upstash (cuenta, credenciales vía env vars) — no es una decisión técnica resoluble sin que el usuario elija el proveedor | M |
| U18 | GlobalPlayer sin i18n ni nombre accesible | `GlobalPlayer.tsx:89-122`: "en cola", "Artículo anterior", "Siguiente artículo", "Velocidad", "Cerrar reproductor" y los tooltips de voz hardcodeados en español (verificado); botón play/pause sin `aria-label` ni `title`. Fix: claves `player.*` en los 5 diccionarios + `aria-label` dinámico | S |
| A4 | Accesibilidad de overlays y estados dinámicos | (1) Dropdowns (`HeaderActions`, kebab de cards) no cierran con Escape ni restauran foco; (2) modal de import sin focus trap ni restauración de foco; (3) **ningún** `aria-live`/`role=status` en la app: error de TTS, pasos de import y "Copiado" no se anuncian | M |
| C7 | Tokens de color inconsistentes | 4 rojos distintos hardcodeados para destructivo/error (`#e53935`, `#ef4444`, `#d93025`) → falta `--color-danger`; `.word-highlight` usa el naranja legacy `rgba(255,109,31)` en vez de `--color-primary-glow` (`#FCA311`); `#f1f1f1`/`#121d37` repetidos para topbars → falta `--bg-topbar`; breakpoints mobile mezclados (600/640/900/901px) | S |
| F24 | Importar archivos PDF, DOCX, TXT y Markdown | Agregar una vía de importación por archivo además de URL/manual. Debe aceptar `.pdf`, `.docx`, `.txt` y `.md`, extraer título/contenido en cliente o servidor según convenga, validar tamaño/tipo, convertir el texto a párrafos reutilizando el flujo actual de guardado/traducción/categorización, y mostrar errores claros cuando el archivo esté vacío, corrupto o sea escaneado sin texto OCR. | M |
| F25 | Importar DOCX desde Google Drive con OAuth | Permitir conectar una cuenta Google vía OAuth, abrir selector/listado de Drive y leer archivos `.docx` desde Drive para convertirlos en artículos. Depende de resolver sesión/OAuth de Google (ver F-1) y requiere permisos mínimos de Drive, manejo de revocación, errores de permisos, archivos grandes/corruptos y reutilizar el flujo actual de guardado/traducción/categorización. | L |
| F27 | Artículos "protegidos" que nunca se borran | Rescatado de `F2` (Favoritos, descartado) pero replanteado: no es una lista de favoritos aparte, es un flag por artículo que lo excluye de cualquier borrado. Requiere: (1) nuevo campo `protected?: boolean` en el tipo `Article` (`src/types.ts`); (2) bloquear el borrado manual en `handleDeleteArticle` (`AppClient.tsx:460`) cuando el artículo tiene el flag — ya sea deshabilitando/ocultando la opción "Borrar" del menú kebab (`card-menu-item card-menu-item--danger`, dos instancias en vista grid/list) o mostrando un mensaje si igual se intenta; (3) excluir los artículos protegidos de la poda automática `pruneArticles` (`AppClient.tsx:965-...`, `MAX_CUSTOM_ARTICLES=50` y el corte por antigüedad de 30 días) con el mismo criterio que ya usa para excluir los artículos default (`defaultIds`); (4) nueva opción "Proteger"/"Desproteger" en el menú kebab de cada card, junto a "Añadir/quitar de cola" y "Borrar"; (5) indicador visual en la card (ej. ícono de candado/pin de Iconoir) para distinguir de un vistazo cuáles están protegidos — decisión tomada con el usuario: sin filtro/sección dedicada aparte, solo el ícono en la card | S |
| F26 | Suscripción a RSS de blogs con importación automática según intereses | El usuario podría suscribirse a feeds RSS de blogs/publicaciones y que Audiodocs traiga artículos nuevos solo, sin pegar cada URL a mano. Hoy el único parseo de RSS que existe es un regex ad-hoc muy acotado al formato del feed de Medium (`tryMediumRSS` en `scrape/route.ts:390-...`, solo extrae `<item>`, no maneja Atom `<entry>` ni namespaces distintos) — esto necesita un parser genérico de verdad (RSS 2.0 y Atom, `content:encoded`, `guid`/`id` para dedup). Piezas a resolver: (1) alta de suscripciones por URL de blog/feed (con auto-discovery del `<link rel="alternate" type="application/rss+xml">` si el usuario pega la URL del sitio en vez del feed directo); (2) mecanismo de chequeo periódico — sin backend con cron hoy (todo es serverless bajo demanda), la opción más simple es revisar los feeds suscritos al abrir la app (mismo patrón que ya usa `pruneArticles`, que corre en cada `fetchArticles`) en vez de un job en background real; (3) "de acuerdo a sus intereses" — filtrar/priorizar qué entradas nuevas importar automáticamente según las categorías que ya usa el usuario (`STATIC_CATEGORIES`/`detectCategory`), o dejarlo en una bandeja de "sugeridos" para aprobar manualmente en vez de autoimportar todo sin pedir permiso; (4) dedup contra artículos ya importados por `guid`/link, igual que hace `tryMediumRSS` con el hash de la URL; (5) reutilizar el flujo existente de guardado/traducción/categorización una vez extraído el texto de cada entrada del feed. Bloqueado en parte por si se quiere sync entre dispositivos de las suscripciones (ver F-1) o alcanza con guardarlas en `localStorage` como el resto de la app hoy | L |
| F21 | i18n de la landing y /registro | La landing de lanzamiento y el formulario de registro (sesión 2026-07-05) quedaron solo en español por decisión de alcance. Cuando el lanzamiento se extienda a otros mercados, migrar sus copys a claves i18n en los 5 diccionarios (hoy están hardcodeados en `src/app/page.tsx` y `src/app/registro/RegistroForm.tsx`, fuera del `LocaleProvider`) | M |
| F-1 | Modalidad con cuenta, OAuth y sincronización en la nube | Mantener dos modalidades independientes: **(1) modo local**, sin cuenta y tal como funciona hoy, con biblioteca, progreso y preferencias guardados solo en el navegador de cada dispositivo; y **(2) modo nube**, opcional, con cuenta mediante OAuth de Google y biblioteca compartida entre dispositivos. En el primer inicio de sesión se debe ofrecer importar y fusionar la biblioteca local con la cuenta, sin pérdida de contenido; una vez activado el modo nube se sincronizan documentos, ediciones, progreso, preferencias de reproducción y cola. Crear una cuenta nunca debe ser obligatorio y el modo local debe seguir disponible. Diseñar la modalidad nube con cuotas/entitlements para que en el futuro pueda convertirse en un plan de pago y habilitar servicios con costo, como mejores voces o traducciones premium, pero dejar pagos y suscripciones fuera de la primera versión. “Local” se refiere al almacenamiento de los datos: scraping, TTS y traducción pueden seguir usando los endpoints actuales. Esta es una iniciativa grande que debe dividirse antes de implementarse en autenticación, persistencia, sincronización/conflictos y gestión del plan. Prerequisito para F25, Gmail import y la versión multiusuario de Alexa (F20) | L |
| F19 | Reproducir artículos en dispositivo Alexa (Echo) | Habilitar una Alexa Skill personal (modo "development" en Alexa Developer Console, habilitada solo en la cuenta propia del usuario — sin certificación ni publicación pública, sin requerir resolver F-1/OAuth) que permita pedirle a Alexa que reproduzca los artículos de la biblioteca. Requiere: (1) persistir el audio generado por `/api/tts` — hoy es 100% efímero, se sintetiza párrafo a párrafo y se descarta (`src/app/api/tts/route.ts`) — en algún storage con URL pública estable por artículo (opción más simple: pre-generar el mp3 completo del artículo al importarlo o bajo demanda, en vez de por párrafo); (2) un endpoint nuevo que implemente la interfaz `AudioPlayer` de Alexa (Alexa no reproduce `<audio>` HTML ni streams arbitrarios, requiere directivas específicas `Play`/`Stop`/`ClearQueue` sobre URLs HTTPS con ciertos requisitos de formato/bitrate); (3) intents básicos (reproducir próximo artículo, pausar, seguir, elegir por categoría) manejados vía Alexa Skills Kit (Lambda o webhook propio); (4) identificar la "cuenta" sin OAuth real, ya que hoy no hay usuarios en el backend — alcanza con un token fijo o un ID de dispositivo dado que la skill es de uso personal, no pública. Queda fuera de alcance (ver F-1, que sigue bloqueado): permitir que otros usuarios de Audiodocs vinculen su propia cuenta Alexa, lo cual sí requeriría OAuth real, account linking, y migrar la biblioteca de `localStorage` a un backend con relación por usuario | L |
| F20 | Versión publicable de la skill de Alexa (multiusuario) | Extensión de F19 para que cualquier usuario de Audiodocs pueda vincular su propia cuenta Alexa, no solo la del dueño de la app. Requiere, además de todo lo de F19: (1) resolver **F-1 primero** (OAuth real) — hoy no hay ningún concepto de usuario en el servidor, la biblioteca vive entera en `localStorage` del navegador (`src/lib/articleStorage.ts`), así que no hay "cuenta" a la cual vincular Alexa; (2) migrar la biblioteca de artículos de `localStorage` a un backend con persistencia real (DB) y relación por usuario, dado que Alexa necesita poder pedir "los artículos de este usuario" desde un servidor, no desde el navegador de nadie; (3) storage en la nube para el audio persistido de F19 pero ahora a escala multiusuario (costo y cuotas por usuario, no solo el propio); (4) account linking real vía OAuth2 en la Alexa Skill (Amazon lo exige para vincular cuentas de terceros, a diferencia del token fijo que alcanza en F19 al ser un solo usuario); (5) certificación de la skill ante Amazon para publicarla en el catálogo público (revisión de políticas, requisitos de UX de voz, manejo de privacidad/datos de terceros) — proceso con vueltas de feedback fuera de nuestro control, no solo trabajo de desarrollo; (6) infraestructura de backend en la nube corriendo 24/7 (hoy todo es serverless/stateless bajo demanda), con los costos y el mantenimiento operativo que eso implica. Bloqueado por F-1. Esfuerzo bastante mayor al de F19 — la escala L de la leyenda se queda corta; es más realista pensarlo como varios ítems L encadenados (OAuth + migración de datos + backend en la nube + certificación) | L |
| F15 | Onboarding para usuario nuevo | 🚧 **BLOQUEADO** — falta que el usuario suba el JSON de artículos de muestra pre-scrapeados por categoría (ver detalle abajo). Pantalla posterior al splash (U10), solo la primera vez, con opción de saltar. Pide nombre (sin OAuth aún, se guarda en localStorage) y preferencias de lectura por selección múltiple sobre `STATIC_CATEGORIES` (`src/lib/categories.ts`). Saludo de bienvenida tipo "te recomendaremos artículos de tus temas de interés para que pruebes, luego podés importar los tuyos". Al terminar, la librería se puebla con artículos de muestra pre-scrapeados (objeto `Article` completo, no URLs a scrapear en vivo) filtrados por las categorías elegidas — requiere un JSON seed nuevo (a proveer) con varios artículos por categoría, similar a `src/data/defaultArticles.ts` pero sí usado para poblar en el primer uso. Requiere key de localStorage propia (distinta a `audiodocs_onboarded` del splash). **Falta:** el usuario debe subir/pasar el JSON con artículos completos (título, autor, párrafos, imagen, categoría) para al menos 2-3 artículos por cada categoría de `STATIC_CATEGORIES` | L |

---

## ✅ Resueltos (no reabrir)

| # | Título | Commit / Sesión |
|---|--------|-----------------|
| B29 | Al terminar el artículo, la lock screen del iPhone queda con "Audiodocs" y favicon pixelado | Causa confirmada: al llegar al fin real de la biblioteca (sin nada más en cola), `speakParagraph`/`playEdgeParagraph` llamaban a `handleStop()`, que pone `playingArticle = null` — eso disparaba el efecto de Media Session, que seteaba `metadata = null` y `playbackState = 'none'` incondicionalmente. iOS no descarta el widget de Now Playing al recibir eso: lo deja visible pero cae a los defaults del PWA (título "Audiodocs", favicon pixelado). Fix: nuevo flag `handleStop({ preserveMediaSession: true })`, pasado solo desde los dos puntos de "fin natural" (no desde los stops explícitos por borrar/editar el artículo, que sí deben limpiar el widget). Nuevo `lastMediaSessionArticleRef` (`AudioPlayerContext.tsx`) retiene la metadata del último artículo cuando el stop la preserva; los dos efectos de Media Session (`metadata` y `playbackState`) caen a ese ref cuando `playingArticle` es `null`, dejando el widget del SO en `playbackState:'paused'` con el título/artista/artwork correctos en vez de nulearse — mismo patrón que usan apps como Podcasts/Spotify al terminar un episodio. Verificado: `tsc --noEmit`/`eslint` limpios; en vivo, instrumentando los setters de `MediaSession.prototype.metadata`/`playbackState` se confirmó que al arrancar la reproducción la metadata se setea correctamente (título/artista del artículo). **No se pudo verificar en vivo la transición de fin natural**: la pestaña de Chrome automatizada usada para probar se quedó colgada reproduciendo audio `data:` (ni error ni `canplay`, confirmado con una prueba aislada fuera de la app — no es una regresión de este fix, la CSP de B39 sigue con `data:` intacta, confirmado con `curl`). Como el bug es 100% del widget de la lock screen de iOS, de todos modos solo se puede confirmar de verdad en un iPhone real — **pendiente que el usuario lo pruebe en el teléfono** | `HEAD` |
| B39 | Incidente en producción: la CSP de S6 rompió la reproducción de todos los artículos | El usuario reportó "play() bloqueado [NotSupportedError]" al intentar escuchar cualquier artículo en `audiodocs.cl`. Causa: `next.config.ts` reproduce el audio de TTS vía **data: URL** (`audioToDataUrl()` en `audioUtils.ts` — convierte el buffer a base64 y arma un `data:audio/mpeg;base64,...`, no un `blob:`), pero la CSP agregada en S6 solo permitía `media-src 'self' blob:` — sin `data:`. El navegador bloqueaba la carga con `MediaError` code 4, mensaje `"MEDIA_ELEMENT_ERROR: Media load rejected by URL safety check"`, indistinguible en la UI de un error genérico de audio. Se había investigado este mismo síntoma durante la sesión de S6 y se concluyó (mal) que era un artefacto del entorno de automatización del navegador — esa prueba usó `URL.createObjectURL()` (blob:, sí permitido) en vez de reproducir el data: URL real que genera el código de la app, dando un falso negativo. Fix: agregar `data:` a `media-src`. Verificado en vivo contra producción: (1) reproducir manualmente el mismo `data:` URL que arma `audioToDataUrl()` reproducía el error exacto reportado, confirmando la causa antes de tocar código; (2) tras el fix, reproducción real de un artículo funcionando (contador de tiempo avanzando, sin errores en consola) | `HEAD` |
| F22 | Importar artículos/hilos de X (Twitter) | Rama dedicada en `scrape/route.ts` (`isTwitterHost`) que salta directo a `tryTwitter()` antes de intentar el fetch normal — x.com/twitter.com nunca sirven texto real sin sesión, así que no vale la pena gastar el timeout del fetch en un intento condenado. Cascada de 3 fuentes, confirmada con el usuario tras evaluar el trade-off: (1) `api.fxtwitter.com/2/thread/{id}` — endpoint "unrolled thread" que **ya devuelve el hilo completo reconstruido en orden** cuando el tweet pedido es parte de uno (verificado en vivo con el hilo real de 31 tweets "How to Get Rich" de @naval, `id=1002103360646823936` → 31 paragraphs en orden); (2) `api.vxtwitter.com` como segundo fallback (solo el tweet suelto, sin hilo); (3) oEmbed oficial de `publish.twitter.com` como último recurso (estable, es de X mismo, pero solo el tweet suelto y sin imágenes — el texto viene embebido en HTML). Las tres fuentes se prueban con el mismo presupuesto de tiempo compartido de R7 (`remaining()`, piso de 1500ms por etapa) para no romper `maxDuration`. Mapeo a `ScrapedRaw`: `author` = `Nombre (@handle)`, `title`/`excerpt` truncados del primer tweet del hilo, `imageUrl` = primera foto encontrada en cualquier tweet del hilo (o thumbnail de video si no hay fotos), `paragraphs` = un tweet por elemento en orden cronológico. **Decisión de producto confirmada con el usuario**: fxtwitter/vxtwitter son proxies no oficiales (scrapean la API interna de X) — pueden bloquearse o desaparecer sin aviso, por eso el fallback a oEmbed en vez de fallar del todo si desaparecen. Reutiliza `safeFetch` (garantías SSRF de S4) y el `MemoryCache` de P5 (mismo mecanismo genérico de caché, sin cambios). Verificado en vivo end-to-end: tweet suelto (`jack/status/20`, con `x.com` y `twitter.com` como host), hilo completo de 31 tweets con categoría/autor/género detectados correctamente, traducción, caché (request repetida 0.04s), y tweet inexistente degradando con gracia a `422 EXTRACT_FAILED` en vez de crashear | `HEAD` |
| P5 | Sin caché de TTS ni de scrape | Nuevo `MemoryCache` genérico (`src/lib/memoryCache.ts`, TTL + tope de entradas con descarte FIFO/LRU-ish) aplicado en ambos endpoints. **TTS**: `synthesizeChunk` (`tts/route.ts`) cachea por `hash(voz+texto)` (SHA-256), 150 entradas / 30 min — re-escuchar un párrafo ya sintetizado no vuelve a golpear Edge TTS. **Scrape**: cachea el resultado final ya procesado (post-Readability, post-traducción) por `hash(url|translateTo)`, 60 entradas / 15 min (TTL más corto que TTS porque acá sí importa la frescura del artículo fuente); solo se cachea el camino feliz completo — nunca errores ni resultados con `translationFailed:true`, para no servir en frío una traducción degradada a la próxima request idéntica. **Limitación conocida y documentada**: el caché vive en memoria de la instancia serverless, no persiste entre lambdas ni cold starts — ver P7, ítem nuevo, para la versión con store compartido (bloqueada por necesitar que el usuario provisione Vercel KV/Blob o Upstash). Verificado en vivo: segunda request idéntica a `/api/scrape` 4.1s→0.4s con la misma respuesta byte a byte; segunda request idéntica a `/api/tts` 3.4s→0.06s con el mismo audio byte a byte | `HEAD` |
| R7 | Sin timeout en `tts.synthesize` ni deadline global del scrape | Nuevo `withTimeout()` compartido (`src/lib/withTimeout.ts`, `Promise.race` contra un timer) porque ni `EdgeTTS.synthesize()` ni el `fetch` de traducción aceptan `AbortSignal` propio. **TTS**: `synthesizeChunk` corta a los 20s (los chunks corren en paralelo vía `Promise.all`, así que el tiempo total ya es ~el del más lento, no la suma) y el endpoint devuelve `TTS_TIMEOUT` (504, con clave i18n `errors.ttsTimeout` en los 5 diccionarios) en vez de esperar a que Vercel mate la función entera en `maxDuration=30` con un 504 sin cuerpo. **Scrape**: nuevo presupuesto de tiempo compartido (`deadline = Date.now() + 27_000`, ~3s de margen bajo `maxDuration`) — la cascada Medium (RSS → archive.org) ahora acota cada etapa a `Math.min(timeoutOriginal, remaining())` en vez de timeouts fijos de 8s/15s sin importar cuánto ya se gastó, con un piso (`MIN_STAGE_BUDGET_MS=1500`) para no intentar una etapa con casi nada de tiempo y devolver `SCRAPE_TIMEOUT` en su lugar; la traducción se corre en paralelo (antes secuencial) bajo el mismo `withTimeout` con el presupuesto restante, y si no alcanza el tiempo, se degrada con gracia: el artículo se guarda en el idioma original (`translationFailed:true`, ver R6) en vez de arriesgar exceder `maxDuration`. Verificado en vivo: TTS y scrape normales sin regresión (200 en ambos); timeout de TTS forzado a 1ms → `504 {"error":"TTS_TIMEOUT"}` en vez de colgarse; deadline de scrape forzado ya vencido con `translateTo` pedido → artículo igual guardado con `translationFailed:true` en vez de fallar la request completa | `HEAD` |
| R6 | Fallos de terceros 100% silenciosos | Los `catch {}` de Google Translate, MyMemory, genderize.io, RSS de Medium y archive.org (`scrape/route.ts`, `translation.ts`) ahora loguean el motivo del fallo (`console.warn`/`console.error` con contexto — status HTTP, target lang, nombre buscado, etc.) en vez de tragárselo en silencio, para poder diagnosticar caídas sistémicas vs. fallos puntuales. Además, `translateText`/`translateConcurrent` (`translation.ts`) devuelven ahora `{ text, failed }` en vez de solo el string — si Google Translate y MyMemory fallan los dos, el texto se conserva en el idioma original (igual que antes) pero el caller se entera. `/api/scrape` y `/api/translate` propagan eso como `translationFailed` en la respuesta JSON; el cliente (`AppClient.tsx`) lo lee tras un import con traducción y muestra un aviso no bloqueante en la vista de éxito del modal ("no se pudo traducir, se guardó en el idioma original") en vez de dejar al usuario pensando que sí se tradujo. Verificado: `tsc`/`eslint` limpios, y traducción real end-to-end contra `/api/translate` confirmando `translationFailed:false` en el camino feliz — la rama de fallo es lógica simple (dos chequeos secuenciales sin estado compartido) verificada por inspección de código, sin necesidad de tumbar servicios externos reales | `HEAD` |
| R5 | localStorage sin resiliencia | Los 3 puntos del ítem original resueltos: (1) `articles` corrupto — `getArticlesList()` y `fetchArticles()` (`AppClient.tsx`) ahora hacen backup del JSON roto bajo `articles_corrupt_backup_<timestamp>` (recuperable a mano desde devtools) y resetean la clave en vez de repetir el mismo fallo en cada carga sin nunca limpiar el estado (`backupAndResetCorruptedArticles`, nuevo en `articleStorage.ts`). (2) `QuotaExceededError` — nuevo `persistArticles()` en `AppClient.tsx` detecta la excepción (`isQuotaExceededError`, cubre `QuotaExceededError` de Chrome/Safari y `NS_ERROR_DOM_QUOTA_REACHED`/code 22/1014 de Firefox y navegadores legacy) y tira un `DisplayError` con mensaje específico (`errors.storageQuotaExceeded`, 5 diccionarios) en vez de cascadear al genérico "Error al importar el artículo" — aplicado en los dos submits (import por URL y manual). (3) Sin listener `storage` — nuevo `useEffect` en `AppClient.tsx` escucha el evento `storage` (que solo dispara en pestañas que *no* hicieron el cambio, nunca en la que escribió — señal correcta sin loop) y llama a `fetchArticles()` para resincronizar cuando otra pestaña modifica `articles`. Verificado en vivo: se corrompió `articles` a mano en localStorage, se recargó la app y se confirmó que arrancó en blanco sin crashear, con el backup exacto guardado bajo la nueva clave y `articles` reseteado a `[]` — luego se restauraron los datos originales | `HEAD` |
| S6 | Headers de seguridad ausentes | Agregado `async headers()` en `next.config.ts`: `Content-Security-Policy`, `X-Frame-Options: DENY`, `X-Content-Type-Options: nosniff`, `Referrer-Policy: strict-origin-when-cross-origin` y `Permissions-Policy` (bloquea cámara/micrófono/geolocalización/pagos/usb, ninguno usado por la app). CSP sin nonce (estática, vía `next.config` en vez de `proxy.ts`) — usar nonce hubiera exigido que **todas** las páginas fueran dinámicas (así lo documenta Next para CSP con nonce), perdiendo el prerender estático de `/`, `/about`, `/registro` y `/app/archive`; se optó por `'unsafe-inline'` en `script-src`/`style-src` para los 2 scripts de arranque de `layout.tsx` en vez de ese trade-off — ver S9, ítem nuevo, para la migración a nonce si se decide que vale la pena más adelante. `img-src`/`media-src` permiten `https:`/`blob:` (los thumbnails vienen de dominios arbitrarios via scraping, y el audio de TTS se reproduce desde `blob:`); `style-src`/`font-src` permiten `fonts.googleapis.com`/`fonts.gstatic.com` (Playfair Display, la única fuente que sigue cargando por `@import` tras C9). Verificado: `tsc --noEmit`, `eslint` y `npm run build` limpios (las páginas que eran estáticas siguen `○ Static`), headers confirmados por `curl -D -`, y sesión en navegador sin violaciones de CSP en consola. **Corrección posterior:** la investigación de un error de reproducción de audio en esa misma sesión concluyó (incorrectamente) que era un artefacto del entorno de automatización del navegador, no la CSP — la reproducción SÍ estaba rota por este cambio, ver B39 | `HEAD` |
| S5 | Rate limiting bypasseable vía spoofing de `X-Forwarded-For` | `getIP` (`rate-limit.ts`) tomaba `split(',')[0]` de `X-Forwarded-For`, un valor que el cliente puede prependear libremente antes de que la request llegue al borde de Vercel — cada request con un IP falso distinto abría un bucket nuevo, sin límite real. Fix: priorizar `x-real-ip`, que en Vercel lo fija el borde según la conexión TCP real y no puede spoofearse desde el cliente (mismo criterio que usa `ipAddress()` del paquete oficial `@vercel/functions`, verificado en su código fuente — se replicó la lógica en una línea en vez de agregar la dependencia). `X-Forwarded-For` queda de fallback solo para local/dev, donde no hay borde de Vercel. Verificado end-to-end: 20 requests a `/api/shorten` con `x-real-ip` fijo pero `x-forwarded-for` distinto en cada una (spoofing simulado) consumen el mismo bucket, y la request 21 responde `429`. La otra mitad del ítem original (store en memoria por instancia serverless) se separó en S8 — requiere que el usuario provisione un store compartido (Upstash/Vercel KV), no es una decisión resoluble sin esa info | `HEAD` |
| U28 | Corrección visual de U26: barra de tabs sin padding, botón Importar sin etiqueta y duplicado en el contenido, fondo blanco del toggle de vista | Reportado por el usuario tras verificar U26 en el teléfono. (1) `.tabs-container` no tenía padding vertical en mobile — al ocultar la barra de tabs y mostrar solo el `<select>` de categorías, la barra quedaba sin aire arriba/abajo; se agregó `padding: 12px 0` en el media query `max-width:600px`. (2) El CTA "Importar" de la barra sticky (`.tabs-import-btn`) quedaba icon-only en mobile por la regla global `.cta-label{display:none}` — se sacó de esa convención y ahora reutiliza el estilo real de `.import-inline-btn` (pill con label siempre visible, en vez del look icon-only de `.view-btn`). Como consecuencia quedaba duplicado con el CTA "Importar documento" que ya vivía junto a los títulos de "Estás escuchando"/"Listo para escuchar" (`AppClient.tsx`) — se eliminaron esas dos instancias redundantes (la barra sticky ya cubre ese caso) junto con la clave i18n `app.importNewArticle`, que quedó huérfana. (3) El botón de vista grid/list (`.view-btn.active`) tenía `background:#FFFFFF` permanente (la clase `active` estaba hardcodeada en el JSX) que ahora, con el CTA de Importar como pill de color al lado, se veía como un cuadrado blanco suelto — se quitó el fondo, dejando solo el cambio de color como feedback | `HEAD` |
| U27 | Zoom accidental en mobile pese a userScalable:false + padding top/bottom de la home | `4fc0511`, verificado en el teléfono |
| U26 | Categorías en dropdown en mobile + CTA de Importar fijo en la barra de tabs | `4fc0511`, verificado en el teléfono |
| B38 | Al retraducir por edición, la voz preferida no cambia de idioma | `4fc0511`, verificado en el teléfono |
| B37 | La barra de accesibilidad/CTA del reader queda visible durante la edición | `4fc0511`, verificado en el teléfono |
| B36 | Sidebar de opciones del reader aparece en blanco si se abre muy abajo en el scroll | `4fc0511`, verificado en el teléfono |
| B35 | Botones Cancelar/Guardar del editor de artículo arriba, con etiquetas largas a dos líneas | `4fc0511`, verificado en el teléfono |
| B34 | Cambiar de voz en reproducción sin detener antes muestra error de audio y reinicia desde el principio | `4fc0511`, verificado en el teléfono |
| B33 | El reproductor permanente cubre la última fila de artículos en la home | `4fc0511`, verificado en el teléfono |
| C9 | Migrar de Font Awesome a Iconoir + tipografía Plus Jakarta Sans | Pedido explícito del usuario. Se desinstaló `@fortawesome/fontawesome-free` (cargaba vía `import` de CSS global en `layout.tsx`, sin CDN) y se instaló `iconoir-react`. Los ~83 usos de íconos en 11 archivos se migraron de glyphs de fuente (`<i className="fa-solid fa-x">`) a componentes SVG de Iconoir, incluyendo los casos con clases dinámicas por estado (play/pause, chevrons de acordeón, compartir/copiado) que pasaron a renderizado condicional de componentes. `IconoirProvider` en el root layout fija `width/height:1em` + `strokeWidth:2` por default para que los íconos sigan escalando con `font-size` igual que los glyphs de FA (la mayoría de los `style={{fontSize:...}}` existentes no necesitaron tocarse). 6 selectores CSS que apuntaban al tag `<i>` (`.dropdown-item`, `.icon-badge`, `.sidebar-toggle-btn` ×3, `.sidebar-note`, más `.playerControls`/`.playButton`/`.bentoIcon`/`.faqIcon`/`.aboutGithub` en `landing.module.css`) migraron a `svg`. Nueva utilidad `.icon-spin` (reusa el `@keyframes spin` ya existente) reemplaza la clase `fa-spin` de FA. En `page.tsx` (landing), los arrays `FEATURES`/`FAQS` guardaban el ícono como string de clase FA — ahora guardan la referencia al componente directamente (`icon: Microphone` en vez de `icon: 'fa-microphone-lines'`) y el render usa `<feature.icon />`. Casos sin match literal en Iconoir: `fa-brands fa-github` → sí existe `Github` en Iconoir (no hizo falta SVG aparte); `fa-moon` y `fa-circle-half-stroke` comparten `HalfMoon` (Iconoir no tiene luna llena/creciente simple en su set `regular`); `fa-newspaper`→`Page` y `fa-layer-group`→`Folder` son las aproximaciones más cercanas disponibles. Tipografía: `next/font/google` con `Plus_Jakarta_Sans({ variable: '--font-sans' })` en `layout.tsx` reemplaza el `@import` de Outfit+Roboto en `globals.css` (Playfair Display, usado solo en `.article-text`, quedó intacto). Verificado con `tsc --noEmit`, `eslint` y `npm run build` limpios, `grep -rn "fa-solid\|fa-brands\|fontawesome" src/` sin resultados, y revisión visual en navegador desktop (claro y oscuro) en home, reader, sidebar de opciones, landing (bento de features, FAQ) y about — íconos y tipografía correctos. **Pendiente verificar en el teléfono** | `HEAD` |
| U29 | Ícono del toggle del panel de opciones del reader hecho "a mano" con CSS (borde + pseudo-elementos) se veía mal | Reemplazado por `SidebarExpand` de Iconoir (parte de C9, encontrado al revisar visualmente la migración). De paso, a pedido del usuario: se sacó la etiqueta "Opciones" del botón que abre el panel y "Volver a la biblioteca" del link de vuelta al listado — ambos quedan solo con el ícono (`ArrowLeft`/`SidebarExpand`), con `title`/`aria-label` para mantener el nombre accesible. CSS muerto eliminado: `.panel-toggle-icon` (+ `::before`/`::after` + overrides de tema oscuro) y `.sidebar-toggle-btn .cta-label`, sin otros consumidores | `HEAD` |
| P4 | `AppClient` recalculaba ~6 `filter`/`sort` sobre `articles` en cada render sin `useMemo` | sesión 2026-07-14: `filteredArticles`, `activeArticles`, `filteredActiveArticles`, `categories`, `listeningArticles`, `newArticles`, `archivedArticles` ahora memoizados con `useMemo` (deps: `articles`/`selectedCategory` según corresponda) en `AppClient.tsx` — dejan de recalcularse en cada re-render disparado por el motor de audio (ej. `setCurrentCharIndex` por palabra). **La otra mitad de P4** (memoizar el `value` de `AudioPlayerContext`) no se pudo resolver de forma segura en esta sesión — ver P4b, nuevo ítem con el detalle de por qué. Verificado con `tsc --noEmit`, `eslint` y `npm run build` limpios | sesión 2026-07-14 |
| B27 | Párrafo >5000 chars rompe el TTS de ese bloque | sesión 2026-07-14: `tts/route.ts` ahora trocea en oraciones (`chunkText`, respeta `.`/`!`/`?`/`\n`, con fallback a corte duro si una oración sola supera el límite) del lado servidor, sintetiza los chunks en paralelo (`Promise.all`) y concatena los buffers de audio en una sola respuesta — el cliente no cambia nada, sigue recibiendo un solo blob de audio. Cap de `MAX_CHUNKS=10` (50000 chars) para no disparar de más contra Edge TTS con texto sin puntuación. `TEXT_TOO_LONG` ahora solo dispara al superar ese cap, no a los 5000 chars originales. Verificado: unit test de `chunkText` (texto corto, largo con oraciones, sin puntuación, exactamente en el límite) y request real end-to-end de 12000 caracteres contra el endpoint (HTTP 200, mp3 válido de 555s confirmado con `ffprobe`) | sesión 2026-07-14 |
| C6 | CSS y código muerto | sesión 2026-07-14: `page.module.css` (143 líneas, sin importar), `src/hooks/useStackedCarousel.ts` (huérfano tras U25), clases sin uso en `globals.css` (`.toast-notification` + keyframe, `.header-search`, `.logo-icon`, `.card-square`, `.grid-new`, `.import-card-*`, `.dialog-*`, `.footer-svg-wordmark` x2, `--gradient-brand`/`--gradient-card`), `clearQueue` sin consumidores (removido del contexto), `onvoiceschanged` ahora se limpia (`= null`) en el cleanup del efecto. **Corrección al ítem original:** `getRemainingTime` sí tenía consumidor real (`GlobalPlayer.tsx:151`) — no se tocó. Verificado con `tsc --noEmit` y `npm run build` exitosos |
| B32 | En mobile el stack de cards se apila al revés con los textos mezclados | `22fc21e` (verificado en el teléfono). Nota: el efecto de naipe que este fix corregía fue reemplazado después por `U25` (carrusel horizontal simple, sin stack) — `useStackedCarousel.ts` quedó huérfano, borrado en C6 |
| U25 | Restaurar cards mobile en carrusel horizontal sin apilado tipo naipe | `HEAD` |
| U24 | Reordenar controles del reproductor con tiempo restante dinámico | `HEAD` |
| U23 | Retraducir artículo desde panel lateral y fijar toolbar del reader al scrollear | `HEAD` |
| U22 | Abrir import automáticamente tras 10s sin interacción en biblioteca vacía | sesión 2026-07-09 |
| U21 | Reproductor siempre visible aunque no haya artículo activo | sesión 2026-07-09 |
| F23 | Editar manualmente artículos importados y retraducir | `HEAD` |
| D2 | Env vars sin documentar | `454d24e` |
| P6 | `updateArticleProgress` reescribe el array completo por párrafo | `45aac23` |
| S7 | `ogTitle`/`ogImage` sin validar en `generateMetadata` | `fc249d0` |
| U20 | `theme-color` fijo en negro | `0ec03ea` |
| U19 | `alert()` como fallback de "link copiado" | `3b03488` |
| B26 | Fallback MyMemory asume origen inglés | `1d7e8da` |
| B23 | Pull-to-refresh duplicado | `60c0a45` |
| S4 | SSRF residual en `/api/scrape`: DNS rebinding + IPv6 mapped | (1) TOCTOU: `assertSafeURL` validaba la IP con `dns.lookup` pero el `fetch` posterior re-resolvía el hostname por su cuenta — un DNS malicioso podía devolver IP pública al validar y privada al fetchear. Fix: `safeFetch` ahora resuelve la IP una sola vez (`resolveSafeIP`) y la "pinea" con un `dispatcher` de `undici` (`Agent` con `connect.lookup` custom) que fuerza la conexión TCP a exactamente esa IP en cada hop de redirect, sin que undici vuelva a resolver el hostname — Host header y SNI de TLS siguen viniendo de la URL original, así que virtual hosting sigue funcionando. Se agregó `undici` como dependencia directa (antes solo transitiva vía `jsdom`, que se desinstaló en C8). Encontrado en el camino: el custom `lookup` necesita manejar la forma `{ all: true }` del callback (Node 20+ habilita Happy Eyeballs/RFC 8305 por default) además de la firma legacy de un solo resultado — sin esa rama, todo fetch fallaba con "Invalid IP address: undefined". (2) `isPrivateIP` no cubría IPv4-mapped (`::ffff:127.0.0.1`, forma hex `::ffff:7f00:1`) ni NAT64 (`64:ff9b::/96`, RFC 6052) — se agregó `extractEmbeddedIPv4` para normalizar ambas formas antes de clasificar. Verificado: IPs privadas siguen bloqueadas (127.0.0.1, localhost, metadata 169.254.169.254), redirect real HTTPS→HTTPS con pin por hop funciona (gitlab.com → about.gitlab.com), scrape real end-to-end exitoso (handbook.gitlab.com), y suite de vectores manual para `isPrivateIP` (mapeado dotted/hex, NAT64, público v4/v6) todos correctos | sesión 2026-07-06 |
| B28 | `hasNext`/`hasPrevious` stale tras import/delete | Memoizados solo por `articleId`; si la lista cambia sin cambiar el artículo en curso, los botones prev/next quedaban mal habilitados. Fix: nuevo `libraryVersion` (contador) + `notifyLibraryChanged()` expuesto por `AudioPlayerContext`, llamado desde los tres write sites de `AppClient.tsx` (mismos de B25) tras cada import/delete — se agrega a las deps del `useMemo` como trigger deliberado de recálculo (no se usa en el cuerpo, sigue evitando el costo de parsear la librería en cada render, ver P1). Verificado en vivo: reproduciendo el penúltimo de 4 artículos (`hasNext=true`), se borró el último sin cambiar de reproducción, y el botón "Siguiente" pasó a `disabled` correctamente sin necesidad de cambiar de artículo | sesión 2026-07-06 |
| B30 | La sidebar de opciones del reader no se cierra al hacer click afuera (desktop) | El `.sidebar-backdrop` que cierra la sidebar con `onClick` solo se renderiza como capa clickeable en mobile (`display:none` desde 901px) — en desktop la sidebar flotaba sin backdrop, así que un click afuera no la cerraba (solo el botón ✕). Fix: mismo patrón de "click outside" ya usado en `HeaderActions.tsx` (listener de `mousedown` a nivel documento) — dos refs (`sidebarRef`, `sidebarToggleRef`) para que el click en el botón toggle no cuente como "afuera" y evitar el double-toggle. No toca el backdrop de mobile (queda igual). Escape para cerrar queda fuera de alcance (ver A4). Verificado en vivo en desktop: click adentro de la sidebar no la cierra, click afuera sí, y el botón toggle sigue abriendo/cerrando sin conflicto | sesión 2026-07-06 |
| B31 | El último tab de categorías queda tapado por el degradado del final | El indicador de scroll de la barra de categorías (`.tabs-scroll-wrapper::after`) es un degradado de 48px fijo pegado al borde derecho, que al scrollear hasta el final quedaba encima del último tab real, atenuándolo. Fix: `.categories-scroll-spacer` (24px, `aria-hidden`) al final de `.categories-scroll` — en el scroll máximo es el spacer el que queda bajo el degradado, no el tab. Verificado en vivo forzando overflow (angostando `.tabs-scroll-wrapper` vía JS) y comparando con/sin el spacer: sin él, "Wirtschaft" se veía parcialmente desvanecido bajo el degradado; con él, queda completamente legible | sesión 2026-07-06 |
| C8 | Dependencia `jsdom` sin uso | `jsdom@29` (pesado) no aparecía en ningún import de `src/` — el scrape usa `linkedom`. Desinstalada junto con `@types/jsdom`. Se hizo en la misma sesión que S4 porque `jsdom` era, sin saberlo, la única razón por la que `undici` estaba disponible en `node_modules` (dependencia transitiva) — al agregar `undici` como dependencia directa para el fix de S4 antes de desinstalar `jsdom`, la remoción queda segura sin arrastrarse `undici` con ella | sesión 2026-07-06 |
| B25 | Pérdida de progreso de escucha al importar/borrar | Los tres puntos de escritura en `AppClient.tsx` (import por URL, import manual, delete) reescribían `localStorage` a partir de su copia en estado `articles`, que queda stale respecto a escrituras directas del player (`articleStorage.ts`: `updateArticleProgress`/`saveArticleVoicePreference`) o del reader (cambio de categoría/voz), pisando `progress`/`lastPlayedAt`/preferencias del artículo en curso. Fix: los tres sitios ahora arrancan de `getArticlesList()` (lectura fresca de `localStorage`, ya existía en `articleStorage.ts`) en vez del estado `articles`, aplican la mutación (agregar/filtrar) sobre esa copia fresca, y recién ahí actualizan estado + `localStorage`. Verificado en vivo: se inyectó `progress` directo en `localStorage` (simulando al player) sin pasar por React state, se borró un artículo distinto desde la UI, y el progreso inyectado sobrevivió intacto | sesión 2026-07-06 |
| B24 | Thumbnail vacío en el player para artículos sin imagen | `GlobalPlayer.tsx` tenía su propia `getGradientClass` devolviendo clases (`gradient-blue-purple`, etc.) que no existen en ningún CSS — las reales son `card-gradient-1..5`, ya usadas por `AppClient.tsx`. Extraída a `src/lib/gradientClass.ts` (misma fórmula de hash que `AppClient`, para que el mismo artículo muestre el mismo degradado en la card y en el mini-thumbnail del player) y ambos archivos ahora importan el helper compartido en vez de tener implementaciones duplicadas/divergentes | sesión 2026-07-06 |
| B22 | Cambiar la velocidad no aplica al párrafo siguiente (stale closure de `speechRate`) | `onended`/`onend` de cada párrafo llaman al siguiente dentro de un closure de función (`playEdgeParagraph`/`speakParagraph`) congelado al render en que arrancó esa reproducción — `toggleSpeed` sí mutaba `audioRef.current.playbackRate` del párrafo en curso (por eso el efecto se sentía "a medias"), pero el siguiente párrafo volvía a leer el `speechRate` viejo capturado en ese closure. Fix: nuevo `speechRateRef` (mismo patrón que `selectedEdgeVoiceRef`) sincronizado por `useEffect`, leído en `utterance.rate` (device, `:233`) y `audioRef.current.playbackRate` (Edge, `:391`) en vez del state directo | sesión 2026-07-06 |
| B21 | Botones anterior/siguiente del reproductor sin feedback cuando no hay más artículos | `handleSkipForward`/`handleSkipBackward` (`AudioPlayerContext.tsx`) ya evitaban saltar fuera de rango, pero los botones en `GlobalPlayer.tsx` no reflejaban visualmente esa limitación — parecían siempre activos aunque no hicieran nada en los extremos de la lista. Se expone `hasNext`/`hasPrevious` desde el contexto (memoizado por `playingArticle?.id`, no en cada render, para no repetir el costo de P1) y los botones ahora usan `disabled` real + estilo atenuado (`.player-btn:disabled`). Validado en vivo en ambos extremos de una librería con 3 artículos | sesión 2026-07-04 |
| U17 | Skeleton al cargar la biblioteca | Reemplaza el `.spinner` genérico por placeholders con la forma real del contenido (tabs, secciones con ícono+título, cards en carrusel) mientras carga, theme-aware con shimmer (`prefers-reduced-motion` respetado). Como la lectura de `articles` desde `localStorage` es síncrona, `isLoading` se agregó un mínimo artificial de 1.5s (`SKELETON_MIN_MS` en `AppClient.tsx`) para que el skeleton llegue a pintarse — sin eso, pasaba de "vacío" a poblado en el mismo tick sin que el usuario llegara a verlo nunca. Bug encontrado y corregido en el camino: el efecto que refresca la sección "Estás escuchando" al cambiar de artículo (`[playingArticle?.id]`) **también se dispara en el montaje inicial** (todo efecto corre una vez al montar sin importar sus dependencias) y llamaba a `fetchArticles()` sin el mínimo de duración, pisando instantáneamente el `isLoading` que el efecto de montaje acababa de setear — el skeleton nunca llegaba a mostrarse pese al delay configurado, ni con 15 segundos de margen. Se agregó un guard (`isInitialMountRef`) para que ese efecto solo actúe en cambios reales de `playingArticle`, no en el montaje. Validado en vivo que el guard no rompe el refresco reactivo del carrusel al reproducir (B20) — sigue siendo instantáneo, sin flash de skeleton | sesión 2026-07-04 |
| U16 | Rediseño visual del sidebar de ajustes del artículo (reader) | El panel de "Opciones" (`articles/[id]/page.tsx`) tenía look genérico y un bug de layering nunca notado: su propio topbar interno (con el título "Opciones") quedaba **tapado por completo** por `.reader-topbar` (z-index 96 vs 95 del `.reader-sidebar`), ambos posicionados al mismo `top: headerHeight` — invisible en desktop y mobile por igual, sin que nadie lo notara porque nunca tuvo contenido interactivo (era solo texto). Fix de stacking: `.reader-sidebar` sube a z-index 97. Aprovechando que ya era visible, se le agregó un botón de cierre real (✕) reusando `.sidebar-close-btn`, una clase CSS que existía en `globals.css` desde antes sin ningún elemento en el JSX que la usara. Rediseño visual: headers de sección con ícono en badge circular tintado (`.icon-badge`, compartida con U15), hover de sección con `var(--bg-card-hover)` en vez de `rgba(0,0,0,0.03)` (casi invisible en dark), selects migrados de estilos inline repetidos x3 a `.sidebar-select` (con la misma flecha custom que `.form-control`), y la nota de advertencia del motor local (`⚠️` embebido como texto en la traducción) reemplazada por `.sidebar-note` con ícono FontAwesome real. A pedido del usuario tras la primera pasada: se quitó la nota de confirmación del motor Edge ("Reproducción de fondo activa..." — clase `.sidebar-note-success` y clave `reader.edgeEngineNote`, ambas eliminadas por no uso), la sección "Ajustes de Reproducción" pasa a expandida por default, la sección "Categoría" ahora es colapsable igual que las otras dos (antes era estática sin chevron), y la fila "Restante" se renombró a "Duración" quitando el prefijo `~` (era el total estimado del artículo, no un contador regresivo real — la clave `reader.remaining` se renombró a `reader.duration` en los 5 diccionarios). Además, `.reader-topbar-inner` ("Volver a la biblioteca" / "Opciones") no quedaba alineado a los bordes reales del viewport como el logo/avatar del header: su padding usaba `max(24px, calc((100vw - 1200px)/2 + 24px))` para recentrar el contenido a un container virtual de 1200px, notorio en pantallas anchas. Ahora usa `24px` fijo, igual que `.header-content`. Validado en vivo en light/dark | sesión 2026-07-03 |
| U15 | Rediseño visual del menú dropdown del avatar | `HeaderActions.tsx`: el dropdown de "Opciones de usuario" (Tema/Importar/Compartir) no tenía animación de entrada, hairlines densas entre ítems (`border-top` por cada uno) y usaba `rgba(0,0,0,0.02)`/hover plano sin relación visual con el resto de la app. Se agregó animación `dropdownIn` (fade + scale), labels de sección en mayúsculas ("Preferencias"/"Acciones", nuevas claves i18n en los 5 diccionarios) reemplazando las hairlines por agrupación semántica, e íconos envueltos en `.icon-badge` (círculo tintado con `--color-primary-glow`) — misma clase reutilizada en el sidebar del reader (U16) para unificar el lenguaje visual entre ambos menús flotantes de la app. A pedido del usuario tras la primera pasada: el botón que abre el selector de idioma (antes una bandera SVG en círculo, de U13) se reemplazó por un recuadro de bordes redondeados con el código de idioma en mayúsculas (`ES`/`EN`/`PT`/`FR`/`DE`), bordeado en `--color-accent` (el azul marino de la paleta — variable que existía en `globals.css` desde antes sin ningún uso real, ya theme-aware: navy en claro, gris claro en oscuro). Las opciones dentro del dropdown conservan sus banderas emoji (decisión de U13, no tocada). Al quedar sin ninguna referencia, se eliminó `public/flags/*.svg` (5 archivos, confirmado con el usuario antes de borrar) | sesión 2026-07-03 |
| B20 | Card desaparecía del carrusel "Estás escuchando" al reproducir un artículo | Al reproducir un artículo (nuevo o viejo) se antepone como primero en `listeningArticles` (ordenado por `lastPlayedAt` desc, `AppClient.tsx`). Si el carrusel `.listening-carousel` ya tenía overflow horizontal, el **scroll anchoring nativo del navegador** (activo por default, nunca desactivado) compensaba el nuevo primer hijo corriendo `scrollLeft` hacia la derecha para mantener el contenido ya visible en su lugar — la card recién activada quedaba fuera del viewport visible del carrusel (sin borrarse del DOM) hasta un refresh completo, que remonta el contenedor con `scrollLeft` en 0. El mismo problema ya había sido mitigado para el carrusel gemelo "Listo para escuchar" (`firstNewArticleId` + `scrollTo` en `AppClient.tsx`), pero nunca se replicó para "Estás escuchando". Fix de dos capas: `overflow-anchor: none` en `.listening-carousel` (`globals.css`) para que el navegador deje de "compensar" solo, más un `useEffect` que resetea `scrollLeft = 0` cuando cambia el primer id de la lista de escucha (para el caso de que el usuario haya scrolleado el carrusel manualmente antes) — sin `behavior: 'smooth'`, porque el scroll animado quedaba a mitad de camino cuando otro re-render (el `fetchArticles()` que dispara el cambio de `playingArticle`) mutaba `scrollLeft` durante la transición. Validado en vivo con overflow real: reproducir cualquier card fuera de la posición 1 la trae al frente sin que desaparezca, tanto para artículos existentes como recién importados | sesión 2026-07-03 |
|---|--------|-----------------|
| U14 | Voces neurales en español priorizan la región mexicana por default | El usuario prefiere acento mexicano sobre español de España. El auto-selector de voz por género del autor (F12/B19) y el fallback por prefijo de idioma se quedaban con el primer match de `EDGE_VOICES`, que tenía las variantes de España declaradas antes que las de México — además, la rama de import con traducción (`isTranslating`) ni siquiera consideraba la voz actualmente seleccionada, así que traducir a "Español" siempre caía en la variante de España sin importar la preferencia del usuario. Se reordenó `EDGE_VOICES` (México antes que España) y se unificó la lógica de matching en `AppClient.tsx` para que ambas ramas (traduciendo o no) prioricen primero la región exacta de la voz actualmente seleccionada, y solo si no coincide con el idioma destino caigan al primer match por prefijo — que ahora es mexicano por default | sesión 2026-07-03 |
|---|--------|-----------------|
| B19 | Cambiar la voz neural no tenía efecto: siempre volvía a sonar con la voz auto-asignada al importar | Causa raíz: `playArticle()` (`AudioPlayerContext.tsx`) reaplicaba incondicionalmente `article.preferredEdgeVoice` en CADA llamada (no solo la primera), y el botón Play del reader siempre pasa el mismo objeto `article` — una foto tomada una sola vez al montar la página (`articles/[id]/page.tsx`) que nunca se actualiza. Como cambiar de voz dispara `handleStop()` (incluso en pausa, porque `isPlaying` sigue en `true`), el siguiente "play" volvía a caer en `playArticle(article, 0)` con ese `article` stale, pisando la selección manual — reproducible tanto cambiando la voz a mitad de reproducción como al revés (detener → cambiar voz → play). Fix: nuevo `currentArticleIdRef` que trackea qué artículo ya tuvo sus preferencias aplicadas en la sesión actual del reproductor (sobrevive a `handleStop`, a diferencia de `playingArticleIdRef`) — `playArticle` solo restaura `preferredEdgeVoice`/`preferredEngine`/`preferredVoiceName` la primera vez que se reproduce ese artículo, no en cada play/resume posterior. De paso se cerró un hueco de persistencia relacionado: `handleEdgeVoiceChange`/`handleVoiceChange` solo guardaban el cambio en `localStorage` si `playingArticle` no era null, así que cambiar la voz con el reproductor detenido no se guardaba — ahora usan `currentArticleIdRef` como fallback. Bug secundario relacionado, también corregido: el auto-selector de voz por género del autor (F12) en `AppClient.tsx` buscaba solo por prefijo de idioma de 2 letras (`'es'`), así que con cualquier voz mexicana seleccionada como default, un autor con género detectado igual resolvía siempre a la variante `es-ES` (aparece primero en `EDGE_VOICES`) en vez de `es-MX` — ahora prioriza la región exacta de la voz actual antes de conformarse con cualquier variante del idioma. Validado en vivo end-to-end en ambas direcciones del bug reportado (mid-playback y "al revés"), incluyendo que la persistencia en `localStorage` quedó correcta | sesión 2026-07-03 |
|---|--------|-----------------|
| B18 | Header y subheader (tabs / barra del reader) no quedaban fijos al hacer scroll | Los tres elementos (`.main-header`, `.tabs-container`, `.reader-topbar`) ya declaraban `position: sticky` correctamente, pero nunca se pegaban. Causa raíz: `body` tenía `overflow-x: hidden` (fix de B14) sin `overflow-y` declarado — el spec de CSS fuerza `overflow-y` a `auto` cuando el otro eje no es `visible`, y eso convertía a `body` en un "scroll container" que nunca scrollea de verdad (su alto siempre iguala al de su contenido); `position: sticky` se ataba a ese contenedor inerte en vez de a `html` (el que realmente scrollea vía `document.scrollingElement`). Se quitó `overflow-x: hidden` de `body` (queda solo en `html`, que ya clippea todo su árbol, incluido el sidebar off-canvas `position:fixed` que motivó B14 — verificado que `scrollWidth` no crece con el sidebar abierto). De paso se corrigió una causa secundaria: `.pull-to-refresh-content` (ancestro común de header/tabs/reader-topbar) tenía `will-change: transform` permanente en el CSS, lo cual en Safari/WebKit mobile también puede romper `position:sticky` en descendientes aunque no haya transform activo — ahora solo se aplica mientras hay un gesto de pull-to-refresh en curso (`PullToRefresh.tsx`). Efecto colateral deseado: como el header vive dentro de ese wrapper, durante el gesto de pull-to-refresh el `transform` activo hace que el header se despegue y se mueva junto con el contenido (no se queda fijo tapando el gesto), y al soltar vuelve a pegarse — sin JS adicional para eso. Validado en Chrome desktop (scroll normal + simulación de drag). **Pendiente:** validar en el teléfono real que no reapareció el scroll horizontal fantasma de B14 en Safari/iOS, ya que ese bug era específico de ese motor y no se pudo reproducir/descartar por completo en Chrome | sesión 2026-07-03 |
|---|--------|-----------------|
| F17 | i18n de mensajes de error dinámicos | Backend (`/api/scrape`, `/api/tts`, `/api/shorten`) ahora devuelve códigos de error estables (`URL_INVALID`, `RATE_LIMITED`, etc., ver `src/lib/i18n/errorCodes.ts`) en vez de texto en español; el cliente los traduce con `t()` vía `translateApiError()` (`src/lib/i18n/apiError.ts`). Se agregaron ~27 claves `errors.*` a los 5 diccionarios (algunas con interpolación — `t()` ahora soporta `params` con placeholders `{nombre}`) y se migraron los mensajes armados en el cliente (`AppClient.tsx`, el reader, `AudioPlayerContext.tsx`). De paso se corrigió una carrera de locale descubierta durante la verificación: en el reader, el mensaje de "artículo no encontrado" se traducía en el efecto de carga, que corre antes que el efecto de `LocaleProvider` que sincroniza el locale real desde `localStorage` — quedaba congelado en español. Ahora se guarda la clave de traducción y se resuelve en el render. Validado en vivo en 3 idiomas (EN/FR/DE) contra flujos reales: URL inválida, rate limit end-to-end, validación manual sin párrafos, y artículo inexistente | sesión 2026-07-02 |
|---|--------|-----------------|
| U13 | Botón de bandera del selector de idioma sin emoji | El botón que abre el dropdown (U12) usaba un emoji de bandera; se reemplazó por banderas SVG propias (`public/flags/{es,en,pt,fr,de}.svg`, colores oficiales) mostradas como `&lt;img&gt;` circular vía `object-fit:cover` + `border-radius:50%`. Las opciones dentro del dropdown mantienen los emojis (a pedido) | sesión 2026-07-02 |
|---|--------|-----------------|
| U12 | Selector de idioma como bandera junto al avatar | Antes vivía como `&lt;select&gt;` dentro del dropdown del avatar (F7); ahora es su propio botón circular con la bandera del idioma actual, ubicado a la izquierda del avatar en el header, con su propio dropdown de banderas + nombre de idioma al tocarlo. `LOCALES` (LocaleContext) ahora incluye `flag` (emoji) por idioma | sesión 2026-07-02 |
|---|--------|-----------------|
| B17 | Voz auto-seleccionada no coincidía con el idioma de traducción del import | La autoselección de voz por género (F12) matcheaba dentro del idioma de la voz *actualmente en uso*, no del idioma al que se tradujo el artículo en este import — si tenías una voz en español seleccionada e importabas traduciendo a inglés, quedaba una voz en español. Ahora usa el idioma de traducción (`effectiveTranslateTo`) cuando el import tradujo, y solo cae al idioma de la voz actual sin traducción. De paso se agregaron las voces neurales en alemán (`Katja`/`Conrad`) que faltaban en `EDGE_VOICES` desde que F18 sumó Alemán como opción de traducción | sesión 2026-07-02 |
| F18 | Portugués, Alemán y Francés en "Traducir a" del import | El backend de traducción (`translateText`/Google Translate/MyMemory) ya era genérico, sin whitelist — solo faltaban las opciones en el `&lt;select&gt;` del modal y sus labels de i18n. Agregadas `modal.langPt/langDe/langFr` a los 5 diccionarios, opciones en `AppClient.tsx`, y la whitelist de F8 (`VALID_TRANSLATE_LANGS`) extendida para que compartir un artículo traducido a estos idiomas también funcione. Validado en vivo: import completo traducido a alemán (234 párrafos) | sesión 2026-07-02 |
| B16 | Modal de import centrado se pierde en mobile con el teclado abierto | En mobile, `.modal-overlay` centraba verticalmente (`align-items:center`); con el teclado del sistema abierto el viewport visual se achica y el modal quedaba parcialmente tapado/inalcanzable. Ahora se ancla arriba (`align-items:flex-start` + padding-top de respiro) y usa `100dvh` (no `100vh`) para el `max-height`, que sí reacciona al teclado en navegadores modernos. Además, `.modal-content` tiene su propio scroll interno (`overflow-y:auto`): se resetea a top con `scrollTo({top:0})` cada vez que cambia de vista dentro del mismo modal (abrir, cambiar de tab, pasar a los pasos de progreso, o al éxito), para que no aparezca ya desplazado si quedó scrolleado de la vista anterior | sesión 2026-07-02 |
| B15 | Import de Medium en dominios custom devolvía contenido truncado en silencio: `uxdesign.cc` y otras publicaciones con dominio propio responden 200 OK con Readability parseando el preview cortado del muro de pago, sin ningún error visible. `isMediumPoweredHtml()` detecta Medium vía meta tags `al:ios:app_name`/`al:android:app_name` (sin depender del host), `isMediumPaywalledHtml()` detecta `"isAccessibleForFree":false` en el JSON-LD, y `getMediumFeedUrl()` prueba `<dominio>/feed` para dominios custom. Validado en vivo con un artículo real recuperado completo (226 párrafos). Para artículos genuinamente member-only sin RSS ni snapshot de archive.org, ahora informa el error en vez de importar contenido cortado | sesión 2026-07-02 |
| B14 | Scroll horizontal feo en el reader: `html`/`body` sin `overflow-x:hidden`, el sidebar off-canvas (`position:fixed` + `translateX(100%)`) generaba scroll horizontal fantasma en Safari/iOS (quirk conocido de esa combinación) | sesión 2026-07-02 |
| F7 | i18n de la interfaz: `LocaleContext` (provider + hook `useLocale` + persistencia `localStorage`) con diccionarios ES/EN/PT/FR/DE en `src/lib/i18n/` (español fuente de verdad, resto validado por tipos vía `satisfies`). Selector de idioma en el dropdown del avatar (`HeaderActions.tsx`), junto al de Tema. Traducidas todas las superficies estáticas: HeaderActions, ThemeSwitcher, Footer, ErrorBoundary (class component, consume el context vía `static contextType`), `about` (dividido en Server Component con metadata + `AboutContent.tsx` client), `AppClient.tsx` completo (biblioteca, cards, modal de import) y el reader (sidebar, hero con fecha locale-aware, controles, compartir/reproducir). Fuera de alcance documentado: mensajes de error dinámicos → ver F17 | sesión 2026-07-02 |
| F13 | Importar artículos Medium member-only gratuitos: `/api/scrape` detecta host de Medium bloqueado por su challenge de Cloudflare y arma cascada `RSS del autor/publicación → archive.org`. El RSS (`medium.com/feed/@usuario` o `/feed/publicación`, derivado de la URL sin fetch previo) entrega el artículo completo sin muro de pago — matcheado por el hash estable al final de la URL/guid, no por el slug (que puede variar). Si tampoco hay snapshot en archive.org, error específico sugiriendo el "friend link" o import manual. Validado en vivo end-to-end contra un artículo real bloqueado (RSS) y contra el mecanismo de snapshot de archive.org por separado | sesión 2026-07-02 |
| F8 | Compartir artículo con idioma de traducción: `Article.translateTo` guarda el idioma con el que se importó (si aplica); `handleShare` del reader agrega `&lang=` al deep link; el auto-import de `AppClient.tsx` (`pendingAutoImportRef`, ahora `{url, lang}`) lee ese `lang` y lo pasa como override a `runScrape` para que el receptor importe ya traducido al mismo idioma | sesión 2026-07-02 |
| F16 | Acortar link de artículo compartido: nuevo endpoint `/api/shorten` (server-side, valida que la URL sea del propio origen antes de pegarle a TinyURL — no queda expuesto como shortener abierto) llamado desde `handleShare` antes de compartir/copiar. Si el acortador falla o tarda (timeout 5s), cae al link largo original sin bloquear el share | sesión 2026-07-02 |
| B12 | AbortError falso en play() del reproductor: la promesa de `.play()` en `setupAndPlay` y en la rama "reanudar" de `handlePlayPause` ahora ignora `AbortError` (interrupción esperada por una acción más reciente que reasigna `src`/llama `pause()`) y descarta callbacks de sesiones de reproducción superadas (`playSessionRef`), en vez de mostrar el mensaje de error aunque la reproducción nueva funcione bien. De paso se agregó el `.catch` que faltaba en la rama "reanudar" | sesión 2026-07-02 |
| B13 | Artwork pixelado en Now Playing / lock screen: cuando el artículo no tiene `imageUrl`, `mediaSession.metadata.artwork` ahora usa explícitamente `/android-chrome-192x192.png` y `/android-chrome-512x512.png` con sus `sizes` declarados, en vez de quedar `undefined` (que hacía que iOS recurriera a un favicon chico estirado) | sesión 2026-07-02 |
| B11 | Voseo argentino en bajada del link compartido: `src/app/(site)/app/page.tsx` — "Escuchá este artículo..." → "Escucha este artículo como podcast en Audiodocs." (español neutro) | sesión 2026-07-02 |
| U11 | Cards en mobile como fichas apiladas: efecto naipe (card angosta al 70vw + solape del 90% vía `useStackedCarousel`, deja asomar solo el borde derecho de la siguiente). z-index dinámico mantiene la card activa al frente durante el scroll. Tocar la porción asomada de una card no activa la trae al frente en vez de abrir el artículo (intercepción de click en fase de captura) — evita entrar directo a una card no totalmente visible. Fix de sombra recortada por el `overflow-x:auto` del carousel (padding+margin compensatorio) | sesión 2026-07-02 |
| B10 | Vista previa de link compartido mostraba marca genérica de Audiodocs en vez del artículo. `/app/page.tsx` se separó en Server Component (con `generateMetadata` dinámica leyendo `?ogTitle=&ogImage=`) + `AppClient.tsx`; `handleShare` en el reader arma el link con esos params tomados del artículo en memoria (sin re-scrapear) | sesión 2026-07-01 |
| F12 | Voz según género del autor: `/api/scrape` detecta género con genderize.io (server-side, en paralelo con traducción/categoría) y el import autoselecciona voz Edge masculina/femenina según el idioma en curso (`EDGE_VOICES` ahora tiene campo `gender`). Solo aplica a importados por URL, no a manuales | sesión 2026-07-01 |
| F11 | Cortina musical al iniciar: jingle.mp3 se reproduce antes del TTS solo al arrancar el artículo desde el principio (no al resumir/saltar párrafos). Timeout de seguridad de 8s por si el archivo no carga/termina, para no bloquear la escucha | sesión 2026-07-01 |
| F14 | Pull-to-refresh: al arrastrar hacia abajo estando en el tope del scroll, ícono/spinner y recarga de la app al soltar (componente PullToRefresh, gesto táctil global) | sesión 2026-07-01 |
| B9 | Barra "Volver a la biblioteca" del reader flotaba separada del header al hacer scroll (position:fixed vs sticky del header). Cambiada a sticky, mismo mecanismo que .tabs-container | sesión 2026-07-01 |
| F9 | Media Session API: metadata (título/autor/imagen) + action handlers (play/pause, anterior, siguiente) en AudioPlayerContext | sesión 2026-06-30 |
| U10 | Splash solo en mobile y solo la primera vez (localStorage `audiodocs_onboarded`) | `HEAD` |
| F10 | Web Share Target: `share_target` en el manifest + auto-import desde `url`/`text`/`title` compartidos | `HEAD` |
| U8 | Etiquetas en botones del reader (Opciones/Compartir) en desktop, icono solo en mobile | `HEAD` |
| U9 | Avatar genérico (fa-user) en vez de letra inicial, sin OAuth | `HEAD` |
| B8 | App no interactiva en el teléfono con el dev server (botones muertos): Next 16 bloquea recursos `/_next` cross-origin desde la IP de la red local → no hidrata. Fix: `allowedDevOrigins` (solo dev) | `HEAD` |
| B7 | Splash: cierre robusto por timer de JS (no solo `onAnimationEnd`) — mejora de robustez, no era el bug del teléfono | `HEAD` |
| B6 | Logo animado del splash cortado en mobile | `HEAD` |
| C3 | Refactor AudioPlayerContext (audioUtils, articleStorage, useQueue) | `fd78b18` |
| U1 | Progreso por tiempo real ponderado por palabras | `main` |
| F5 | OpenGraph + metadataBase en root layout | `main` |
| F6 | Fallback MyMemory cuando Google Translate falla | `main` |
| B5 | Delete no elimina artículos | `d8138db` |
| U7 | Confirmación de delete: overlay sobre la card (dark/light) | `ff13e57` |
| U3 | Auto-scroll vs scroll manual en reader | `fix/u3-autoscroll` |
| P2 | Memoizar parseTokens en reader | `89e18e2` |
| B1 | Voice stale en double-buffer | `94d21c9` |
| B2 | Race condition engine al cambiar artículo | `94d21c9` |
| R2 | TTS sin timeout ni retry visible | `a9aa5a9` |
| C5 | localStorage sin validación de esquema | `fd06f05` |
| P1 | fetchArticles en cada párrafo | sesión 2026-06-25 |
| P3 | prefers-reduced-motion en cardPan | sesión 2026-06-25 |
| U2 | window.confirm → confirmación dos pasos | sesión 2026-06-25 |
| U4 | Paso "Traduciendo texto" en import | sesión 2026-06-25 |
| C1 | EDGE_VOICES unificado en AudioPlayerContext | sesión 2026-06-25 |
| L1 | Lint 0 errores | sesión 2026-06-25 |
| A1 | Contraste text-muted #595959 | sesión 2026-06-25 |
| A2 | :focus-visible en player | sesión 2026-06-25 |
| A3 | role=dialog + aria-modal en modal | sesión 2026-06-25 |
| F-2 | Cola de reproducción completa | sesión 2026-06-25 |
| S1 | SSRF en scraper | pre-sesión |
| S3 | TTS vía GET expone texto en URL | pre-sesión |
| R1 | Timeout scraper | pre-sesión |
| R3 | Stack trace expuesto en errores | pre-sesión |
| R4 | Validación de tamaño de archivo | pre-sesión |
| B3 | Blob leak en reproductor | pre-sesión |
| U5 | Categorías vacías visibles | pre-sesión |
| U6 | Font Awesome vía CDN externo | pre-sesión |
| C4 | Pruning inicial de código muerto | pre-sesión |
| F0 | PWA icons | pre-sesión |
| D1 | README | pre-sesión |

---

## 🚫 Descartados

| # | Título | Razón |
|---|--------|-------|
| F1 | Import por lotes | No se hará |
| F3 | Estadísticas | No se hará |
| F4 | Offline / IndexedDB | Solo al final de todo cuando no quede nada más |

---

**Leyenda de esfuerzo:** XS (<1h) · S (1-3h) · M (medio día) · L (1-2 días)
